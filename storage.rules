rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow profile picture uploads for authenticated users
    match /profile-pictures/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
